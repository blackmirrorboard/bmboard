# CONVENTIONS — Black Mirror Board / kinoshita studio

> このファイルは Aider が `--read` で自動読込する「設計思想の憲法」。
> ローカルAI（Qwen2.5-Coder）は **書くすべてのコードをこの原則に通してから出力すること。**

---

## 0. 絶対遵守の憲法（The Constitution）

1. **引き算が正義。** 機能を足すより、既存を削れないか先に考える。
2. **依存ゼロ。** `package.json` を追加してはいけない。Vanilla JS / HTML / CSS のみ。
3. **1ファイル主義。** 原則として `app.html` 一枚で完結する。モジュール分割の提案は禁止。
4. **モノクローム。** 色を追加するときは必ず理由を説明。グレースケール + 許可された1色（`#1600A2` cobalt）以外は使わない。
5. **黙って書くな。** 変更する前に「何を / なぜ / 何を削るか」を3行で宣言すること。

---

## 1. 美学（kinoshita studio aesthetic）

### 1bit Principle
- 情報は白/黒の二値で伝える。グレーは補助線に限る。
- Canvas 背景: `#FAFAF8`（冷白ではなく紙の温度）
- Terminal 背景: `#080808`（純黒ではなくインクの重み）
- Terminal テキスト: `#F5F2EC`（純白ではなくリン光）
- Dim テキスト: `#707070`
- 許可された有彩色は Gray モードの `#1600A2` のみ

### テクスチャ
- Scanline（走査線）: デジタルに物理性を与える CRT の記憶
- Film grain / noise: 静寂の質感。過剰にすると美学が壊れる

### タイポグラフィ
- 等幅フォント優先（ターミナル由来）
- 日本語と英語を混在させる詩的な短文。装飾語を削る。
- 見出しは動詞または体言止め。形容詞は最小。

---

## 2. コンセプト（BIWAKO SILENCE の思想）

- **体験翻訳:** 身体的な行為（描く）をデジタルな結果（コマンド）に変換する。
- **静寂の再定義:** ノイズを削ることで「残された一音」を際立たせる。
- **非対称性の価値:** 入力（自由・即興）と出力（構造・再現可能）のギャップが高揚感を生む。
- **Causality < 200ms:** 行為とフィードバックの遅延はこれを超えないこと（Michotte の因果性知覚）。

---

## 3. コーディング規則

### 言語
- HTML / CSS / Vanilla JavaScript のみ
- ES2020+ 機能は OK。Babel / TypeScript / バンドラは禁止。

### 命名
- 関数名は動詞句（`renderCanvas`, `castSpell`）
- 定数は SCREAMING_SNAKE_CASE
- CSS カスタムプロパティは `--ink`, `--paper`, `--dim` のような意味名（色名ではなく）

### コメント
- 原則としてコメントは書かない。名前が説明する。
- 例外: 非自明な物理定数、ヒステリシスしきい値、仕様バグの回避。
- 絶対禁止: 「〜を追加しました」「TODO: ...」「XXX のために」系のメタコメント

### ファイル操作（Aider への指示）
- **既存ファイルを優先編集。** 新規ファイルは提案前に理由を説明。
- **削除OKなコードは削除する。** 「後方互換」「将来のため」は禁句。
- `.md` ファイル（README / designer.md / ux.md 等）を編集するときは、既存の見出し構造を維持。

---

## 4. Aider とのやり取りの型

- 変更を開始する前に **3行宣言**：
  1. 何を変えるか
  2. なぜ変えるか（ユーザーが得る価値）
  3. 何を削るか（引き算の確認）
- 迷ったら手を止めて「これはスタジオの署名か、ユーザーの道具か？」を問う。
  - **スタジオの署名** → 美学を厳守
  - **ユーザーの道具（fetch-img, export 等）** → 美学を下げて素直に使いやすく

---

## 5. やってはいけないこと（Do Not）

- フレームワーク追加の提案（React / Vue / Svelte / Tailwind など）
- ビルドステップの追加
- コメントアウトされたコードの温存
- 「改善のため」の命名変更（ユーザーが依頼していない限り）
- モーダル / ツールチップの多用（静寂を壊す）
- デフォルトの青リンク色、素のブラウザ UI の露出
- 絵文字の多用（コード・UI・コミットメッセージ全てで）
