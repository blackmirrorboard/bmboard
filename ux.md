# UX Researcher

**Role:** 描画とコマンドの心理的同期設計・「高揚感」の言語化・アフォーダンス設計
**Doc version:** v1.0 (2026-04-16)

---

## v1.0 — 「狙って掴む」から「直感的に掴める」へ

v0.9 までのユーザーフィードバックで最大の摩擦点は、**細い線を正確にクリックしないと掴めない**ことだった。
v1.0 の Hit-Test 2.0 はこれを解決する。

- 全ての線に **見た目の線 + 8px の見えないパディング** が付与されている。
- 鉛筆ストロークは点ベースではなく **セグメント距離** で判定されるため、簡略化された頂点間のギャップにも反応する。
- マウスがオブジェクトの判定領域に入ると、**点線のバウンディングヒント** と `move` カーソルが同時に発火 → 掴める状態を明示。

これは Fitts's law の実装と、Nielsen の *Visibility of system status* のハイブリッド。
「線を探す」認知コストが消え、ユーザーは「思った場所を掴む」ができるようになった。

---

## 核心的な問い

> 「描く」という行為はなぜ「打つ」より高揚感を生むのか。

手書きは身体的・即興的。タイピングは意図的・計算的。  
この非対称性こそが Black Mirror Board の価値の源泉。

---

## 理論的根拠

### フロー理論（Csikszentmihalyi, 1990）
- スキルと挑戦のバランスが「没入」を生む
- 描画は敷居が低い（スキル不要）にもかかわらず、アウトプットが複雑（コード）
- この非対称性が「想定外の達成感」をつくる

### 因果性の知覚（Michotte, 1946）
- 行為とフィードバックの遅延が **< 200ms** の場合、因果関係として認知される
- ターミナルへの反映は描画終了後 300〜500ms 以内に開始することが必須
- 遅すぎると「ツール」ではなく「マジック」になる（悪い意味で）

### アフォーダンス（Gibson → Norman）
- 白いキャンバスは「描くこと」をアフォードする
- 黒いターミナルは「読むこと」をアフォードする
- この対比がユーザーを「作者」と「観察者」の両方にする

---

## フィードバック設計の優先順位

1. **Pen スナップリング** — 青いリングが始点に近づいたことを視覚的に通知。クローズ完了を「起こりそう」から「起きた」へ明確に分離する
2. **タイピング速度の演出** — ターミナルに文字が流れる速度は人間の最高タイピング速度を超えた「非人間的速度」にする
3. **コマンドとの意味的整合性** — ○ は init/create 系（起点）、→ は pipe/curl 系（流れ）、◎ は deploy 系（展開）
4. **画像配置のフィードバック** — 配置直後にターミナルへ `[IO] image upload — filename` ログを出力。機械が「見た」ことを伝える

---

## 意味論マトリクス

| 図形 | 意味 | コマンド系統 |
|------|------|-------------|
| ○ | 起点・誕生 | git init, npm create, http.server |
| ◎ | 展開・昇格 | docker run, kubectl apply |
| △ | 分岐・選択 | git branch, if/else |
| □ | 囲い・コンテナ | docker build, terraform |
| → | 流れ・変換 | curl \| jq, grep \| sort |
| ✎ closed | 境界・領域定義 | namespace, scope, module |
| 🖼 | アセット挿入 | cp, scp, rsync |

---

## v0.9 での解決済み UX 課題

| 課題 | 解決 |
|------|------|
| モバイルでポップオーバーが画面外に消える | `getBoundingClientRect()` + ボタン下方向への配置ロジック |
| タッチ操作で誤ってブラウザズームが発動 | `gesturestart/change/end` の `preventDefault` + `touch-action: manipulation` |
| アンカーポイントが小さくてタッチできない | タッチ時に当たり判定を ×2.2 に拡大（Fat Finger 対応） |
| Pen を閉じた後に Fill が適用できない | `closed` フラグと `fill` プロパティを stroke オブジェクトに追加 |
| 選択オブジェクトを外部に渡せない | X キーで透過 PNG エクスポート（タイトクロップ・Retina） |
| モバイルでキャンバスとターミナルの比率が固定 | 縦ドラッグハンドルで自由リサイズ |
| ロゴがテキストで識別性が低い | logo.png 差し替え + フォールバックテキスト |

---

## 未解決 / 継続観察

- YouTube イースターエッグは 1 回の驚きで終わる。2 回目以降の体験設計が必要
- 「なぜこのコマンドなのか」が説明なく分かるための、命名・出力のリアリティ精度
- 画像オブジェクトのロテーション UX — アンカードラッグでの直感的な回転は未実装

---

## 次の観察ポイント（v1.0 候補）

- [ ] 初回ユーザーがどのツールを最初に触るか（ヒートマップ観察）
- [ ] Pen クローズの成功率 — スナップ距離の最適値は 22px か、それ以上か
- [ ] 画像配置後にユーザーが行う最初のアクション（移動 / リサイズ / 別ツール）
- [ ] ターミナル出力を「読んでいる」ユーザー比率の観察
- [ ] フィードバックモーダルのコンバージョン率（ロゴタップ → メール送信）

---

*Draw to Build. — kinoshita studio / 2026*
